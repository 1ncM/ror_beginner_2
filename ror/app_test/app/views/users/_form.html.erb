<%= form_for(@user) do |f| %>
  <%= render 'common/errors', resources: @user %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit @user.new_record? ? 'Создать' : 'Обновить' %>
  </div>
<% end %>
