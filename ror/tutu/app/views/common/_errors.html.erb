<% if resources.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resources.errors.count, "error") %> prohibited this resources from being saved:</h2>

      <ul>
      <% resources.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>