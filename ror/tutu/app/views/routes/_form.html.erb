<%= form_for(@route) do |f| %>
  <%= render 'common/errors', resources: @route %>


  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
